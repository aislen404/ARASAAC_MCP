# Design — 0015-preview-editor

La preview reutiliza el estado local seleccionado y el material devuelto. Las
imágenes se cargan solo desde `static.arasaac.org`, con alt y texto. Reordenar se
hace con botones, no drag-only.
