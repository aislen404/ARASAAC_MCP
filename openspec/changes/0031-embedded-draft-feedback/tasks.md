# Tasks — 0031

## 1. OpenSpec

- [x] Crear `proposal.md`, `design.md`, `tasks.md`, `spec.md`.

## 2. Feedback embebido

- [ ] Mostrar `message` accesible en `editor-panel.tsx` junto a `Crear borrador`.
- [ ] Mostrar `message` también en `review-panel.tsx` cuando `embedded = true`.
- [ ] Mantener estado visible tras crear borrador.

## 3. Tests

- [ ] Añadir o actualizar tests del flujo embebido para `Crear borrador`.

## 4. Validación

- [ ] Ejecutar checks del frontend afectados.
